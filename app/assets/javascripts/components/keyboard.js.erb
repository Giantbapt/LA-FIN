document.onkeydown = checkKey;

function checkKey(e) {

  console.log('you press a key');
  var article = document.getElementById('image-show');
    e = e || window.event;

    if (e.keyCode == '37') {
        console.log("hello bapt");
        console.log(article.dataset.left);
        // window.location.replace("http://www.w3schools.com")

    }
    else if (e.keyCode == '39') {
        // down arrow
    }

}
