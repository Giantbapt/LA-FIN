var jon = document.querySelectorAll('.flip-card');
var jonShow = document.querySelectorAll('.flip-card-show');
console.log('hello');
function returnCard (card){
    card.classList.remove('flip-card-out');
    card.classList.remove('flip-card-in');
    card.classList.add('flip-card-in');
    console.log("you are in");

}
function returnCardInfinite (card) {
    var number = card.dataset.number
    var titre = card.dataset.titre
    var dimensions = card.dataset.dimensions
    var details = card.dataset.details

    var numberInfos= document.querySelector('.number');
    var titreInfos= document.querySelector('.titre');
    var dimensionsInfos= document.querySelector('.dimensions');
    var detailsInfos= document.querySelector('.details');
    card.addEventListener('mouseenter', function(){
        console.log("hello");
        card.classList.remove('flip-card-in');
        card.classList.remove('flip-card-out');
        card.classList.add('flip-card-out');
        numberInfos.innerHTML = number;
        titreInfos.innerHTML = titre;
        dimensionsInfos.innerHTML = dimensions;
        detailsInfos.innerHTML = details;
    })
    card.addEventListener('mouseleave', function(){
        setTimeout(()=> {
            card.classList.remove('flip-card-out')
            card.classList.add('flip-card-in')
            console.log("goodbye");
        }, 5 * 1000);
    })
}

window.addEventListener('load', (event) => {
    console.log("Hello");

    // sélectionner toutes les flip-cards
    // réaliser une action pour chacune de ces flip card
    // créer un array de 100 valeurs
    jon.forEach(function(card, index) {

        setTimeout(function() {returnCard(card);}, 50 * index);
    })

     jon.forEach(function(card) {

        setTimeout(function() {returnCardInfinite(card);}, 50 * 100);
     })
    //  jon.forEach(function(card, index) {
    //     touchTurnCard(card);
    //     debugger
    // })

    //  jon.forEach(function(card) {
    //     setTimeout(function() {touchReturnCardInfinite(card);}, 50 * 100);
    //  })

})

jonShow.forEach(function(card) {
    var number = card.dataset.number
    var titre = card.dataset.titre
    var dimensions = card.dataset.dimensions
    var details = card.dataset.details

    var numberInfos= document.querySelector('.number');
    var titreInfos= document.querySelector('.titre');
    var dimensionsInfos= document.querySelector('.dimensions');
    var detailsInfos= document.querySelector('.details');

    card.addEventListener('mouseenter', function(){
        numberInfos.innerHTML = number;
        titreInfos.innerHTML = titre;
        dimensionsInfos.innerHTML = dimensions;
        detailsInfos.innerHTML = details;
    })
})



// function startup() {
//   var el = document.querySelector(".flip-cards");
//   // el.addEventListener("touchstart", handleStart, false);
//   // el.addEventListener("touchend", handleEnd, false);
//   // el.addEventListener("touchcancel", handleCancel, false);
//   el.addEventListener("touchmove", handleMove, false);
// }

// document.addEventListener("DOMContentLoaded", startup)


// var ongoingTouches = [];

// function handleMove(evt) {
//   evt.preventDefault();
//   var el = document.querySelector(".flip-cards");
//   // var ctx = el.getContext("2d");
//   var touches = evt.changedTouches;
//   console.log(touches)


//   for (var i = 0; i < touches.length; i++) {
//     var color = colorForTouch(touches[i]);
//     var idx = ongoingTouchIndexById(touches[i].identifier);

//     if (idx >= 0) {
//       console.log("continuing touch "+idx);
//       ctx.beginPath();
//       console.log("ctx.moveTo(" + ongoingTouches[idx].pageX + ", " + ongoingTouches[idx].pageY + ");");
//       ctx.moveTo(ongoingTouches[idx].pageX, ongoingTouches[idx].pageY);
//       console.log("ctx.lineTo(" + touches[i].pageX + ", " + touches[i].pageY + ");");
//       ctx.lineTo(touches[i].pageX, touches[i].pageY);
//       ctx.lineWidth = 4;
//       ctx.strokeStyle = color;
//       ctx.stroke();

//       ongoingTouches.splice(idx, 1, copyTouch(touches[i]));  // swap in the new touch record
//       console.log(".");
//     } else {
//       console.log("can't figure out which touch to continue");
//     }
//   }
// }
// var cards = document.querySelector('.flip-cards');
// cards.addEventListener('touchstart', function(evt){
//         var alltouches = [];
//         alltouches += evt.touches ;
//         console.log(evt.touches);
//         console.log(alltouches);
//     })


// document.addEventListener('touchmove', function(){
//     console.log('hello')
// })
// // configure au click le début de l'action.
// // function twistCard (card){
// //     card.classList.remove('flip-card-out');
// //     card.classList.remove('flip-card-in');
// //     card.classList.add('flip-card-out');
// //     console.log("you are twisted");
// //     // debugger
// // }
// function touchTurnCard (card){
//     // card.ontouchmove = twistCard(card);
//     card.classList.remove('flip-card-out');
//     card.classList.remove('flip-card-in');
//     card.classList.add('flip-card-in');

//     console.log("you are in the touch band");

//     setTimeout(()=> {
//             card.classList.remove('flip-card-out')
//             card.classList.add('flip-card-in')
//             console.log("goodbye");
//     }, 5 * 1000);
// }


// // function touchReturnCardInfinite(card) {
// //     // jon.addEventListener('touchstart', function(){
// //     //     jon.forEach(function(card) {
// //            var number = card.dataset.number
// //             var titre = card.dataset.titre
// //             var dimensions = card.dataset.dimensions
// //             var details = card.dataset.details

// //             var numberInfos= document.querySelector('.number');
// //             var titreInfos= document.querySelector('.titre');
// //             var dimensionsInfos= document.querySelector('.dimensions');
// //             var detailsInfos= document.querySelector('.details');


// //             card.addEventListener('touchmove', function(e){
// //                 console.log(e.targetTouches);
// //                 card.classList.remove('flip-card-in');
// //                 card.classList.remove('flip-card-out');
// //                 card.classList.add('flip-card-out');
// //                 numberInfos.innerHTML = number;
// //                 titreInfos.innerHTML = titre;
// //                 dimensionsInfos.innerHTML = dimensions;
// //                 detailsInfos.innerHTML = details;
// //                 console.log("hello touch move");
// //                 e.preventDefault()
// //             })
// //             card.addEventListener('touchend', function(){
// //                 setTimeout(()=> {
// //                     card.classList.remove('flip-card-out')
// //                     card.classList.add('flip-card-in')
// //                     console.log("goodbye");
// //                 }, 5 * 1000);
// //             })
// //     //     })
// //     // })
// // }












// console.log("hello fucker");

// // const element = document.querySelectorAll(".flip-card-front");
// // console.log(element)
// // element[0].addEventListener('mouseover', (event) => {
// //   console.log("hello!");
// // });
// // function addHover() {
// //   element.addEventListener("mouseover", (event) => {
// //   console.log("hello!");
// // });
// // }

// addHover();
//export { addHover } ;
// document.querySelectorAll('.flip-card').forEach(function(card) {
//     var number = card.dataset.number
//     var titre = card.dataset.titre
//     var dimensions = card.dataset.dimensions
//     var details = card.dataset.details

//     var numberInfos= document.querySelector('.number');
//     var titreInfos= document.querySelector('.titre');
//     var dimensionsInfos= document.querySelector('.dimensions');
//     var detailsInfos= document.querySelector('.details');
//     card.addEventListener('mouseenter', function(){
//         console.log("hello");
//         card.classList.remove('flip-card-in');
//         card.classList.remove('flip-card-out');
//         card.classList.add('flip-card-out');
//         numberInfos.innerHTML = number;
//         titreInfos.innerHTML = titre;
//         dimensionsInfos.innerHTML = dimensions;
//         detailsInfos.innerHTML = details;
//     })
//     card.addEventListener('mouseleave', function(){
//         setTimeout(()=> {
//             card.classList.add('flip-card-out')
//             console.log("goodbye");
//         }, 5 * 1000);
//     })
// })

//Utilisation
//La variable contient un nombre aléatoire compris entre 1 et 10

//  jon.forEach(function(card, index) {
//             //attendre 0,5s
//             // retourner la carte
//                 setTimeout(function() {returnCard(card);}, 50 * index);
//             // debugger
// })


// var displaySequence = function(){
//     compSequence.forEach(function(color, index){
//         setTimeout(function(){
//             $("#" + color).fadeTo(300, 0.5).fadeTo(300, 1.0);
//         },
//         1000 * index);
//     })
// }
// const delayLoop = (fn, delay) => {
//   return (name, i) => {
//     setTimeout(() => {
//       display(name);
//     }, i * 1000);
//   }
// };
// names.forEach(delayLoop(display, 1000));


    // <#% portraits.each_slice(10) do |portrait_slice| %>
    //         console.log("hello");
    // <#%    portrait_slice.each do |portrait| %>
    // <#%      portrait.classList.add('flip-card-in') %>
    // <#%      end %>
    // <#% end %>




    // document.querySelectorAll('.flip-card').forEach(function(card) {
    //     var number = card.dataset.number
    //     var titre = card.dataset.titre
    //     var dimensions = card.dataset.dimensions
    //     var details = card.dataset.details

    //     var numberInfos= document.querySelector('.number');
    //     var titreInfos= document.querySelector('.titre');
    //     var dimensionsInfos= document.querySelector('.dimensions');
    //     var detailsInfos= document.querySelector('.details');
    //     setTimeout(5000);

        // card.addEventListener('mouseenter', function(){
        // 	// console.log("hello");
        //     card.classList.remove('flip-card-out');
        //     card.classList.remove('flip-card-in');
        //     card.classList.add('flip-card-in');
        //     numberInfos.innerHTML = number;
        //     titreInfos.innerHTML = titre;
        //     dimensionsInfos.innerHTML = dimensions;
        //     detailsInfos.innerHTML = details;
        // })
    //    //  card.addEventListener('mouseleave', function(){
    //    //      setTimeout(()=> {
    //    //      	card.classList.add('flip-card-out')
    //    //  		console.log("goodbye");
    //  		// // }, 5 * 1000);
    //    //  })
    // })
// })

// document.querySelectorAll('.flip-card-show').forEach(function(card) {

//     var number = card.dataset.number
//     var titre = card.dataset.titre
//     var dimensions = card.dataset.dimensions
//     var details = card.dataset.details

//     var numberInfos= document.querySelector('.number');
//     var titreInfos= document.querySelector('.titre');
//     var dimensionsInfos= document.querySelector('.dimensions');
//     var detailsInfos= document.querySelector('.details');
//     card.addEventListener('mouseenter', function(){
//         numberInfos.innerHTML = number;
//         titreInfos.innerHTML = titre;
//         dimensionsInfos.innerHTML = dimensions;
//         detailsInfos.innerHTML = details;
//     })
//     card.addEventListener('mouseleave', function(){
//         // var infos = document.querySelector('.index-titre');
//         // console.log(infos.value);
//         // infos.remove(id);
//     })
// })

